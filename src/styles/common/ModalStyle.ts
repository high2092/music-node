import styled from '@emotion/styled';
import { MODAL_DEFAULT_BORDER_RADIUS } from '../../constants/style';

export const ModalStyle = styled.div`
  padding: 2rem;
  background-color: white;

  border-radius: ${MODAL_DEFAULT_BORDER_RADIUS};

  color: black;
`;
